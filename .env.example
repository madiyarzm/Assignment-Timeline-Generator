# Example .env file
# Copy this to `.env` in your local development environment and fill in the real key.
# DO NOT commit your real `.env` to version control.

OPENAI_API_KEY=your-openai-api-key-goes-here
